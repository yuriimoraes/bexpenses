@model IEnumerable<BExpensesModeloDDD.MVC.ViewModels.TipoDespesaCarroViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoDespesaCarro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoDespesaCarro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoDespesaCarroID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoDespesaCarroID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoDespesaCarroID })
        </td>
    </tr>
}

</table>
