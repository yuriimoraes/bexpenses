@model IEnumerable<BExpensesModeloDDD.MVC.ViewModels.TipoAcomodacaoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoTipoAcomodacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoTipoAcomodacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoAcomodacaoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoAcomodacaoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoAcomodacaoID })
        </td>
    </tr>
}

</table>
